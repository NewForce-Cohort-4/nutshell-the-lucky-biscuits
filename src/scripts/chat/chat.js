export const htmlChat = (chat) => {
    
    return `
    <br>
             <p>${chat.userName}:&nbsp       ${chat.time}</div>
             <div>&nbsp${chat.chat}</div}
             
            
    `
}